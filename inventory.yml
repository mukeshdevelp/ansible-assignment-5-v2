---
# Static inventory for SonarQube deployment
# Test server configuration
all:
  children:
    web-server:
      hosts:
        test_server:
          ansible_host: ec2-3-83-30-77.compute-1.amazonaws.com
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ./mukesh_sonarqube.pem

